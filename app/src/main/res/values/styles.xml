<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="MenuButtonsStyle">
        <item name="fab_size">mini</item>
        <item name="fab_showAnimation">@anim/jump_from_down</item>
        <item name="fab_hideAnimation">@anim/jump_to_down</item>
        <item name="fab_shadowColor">@color/fabShadow</item>
        <item name="fab_colorNormal">@color/fabNormal</item>
        <item name="fab_colorPressed">@color/fabPressed</item>
        <item name="fab_colorRipple">@color/fabRipple</item>
    </style>

    <style name="MapImageViewWithRightMargin">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginRight">@dimen/aspect_image_right_margin</item>
        <item name="android:background">@drawable/aspect_selector</item>
        <item name="android:padding">@dimen/aspect_image_view_padding</item>
        <item name="android:onClick">onAspectClick</item>
        <item name="android:visibility">invisible</item>
    </style>

    <style name="MapImageViewWithoutRightMargin">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/aspect_selector</item>
        <item name="android:padding">@dimen/aspect_image_view_padding</item>
        <item name="android:onClick">onAspectClick</item>
        <item name="android:visibility">invisible</item>
    </style>

    <style name="MapSpacer">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>